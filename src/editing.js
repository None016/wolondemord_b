import React from "react";

class Editing extends React.Component{
    render(){
        return(
            <h1>Редактирование файла: {this.props.idFile}</h1>
        );
    }
}

export default Editing

